import { Injectable } from '@nestjs/common';

/**
 * Root application service placeholder.
 *
 * Kept intentionally empty while the API acts as a scaffold for the
 * landing-page scope (Requirement 21.5). Business logic belongs in
 * feature-specific services.
 */
@Injectable()
export class AppService {}
