import { Controller } from '@nestjs/common';

/**
 * Root controller placeholder.
 *
 * Intentionally empty at the scaffold stage (Requirement 21.5). The
 * `HealthController` is added in task 3.2 under its own module.
 */
@Controller()
export class AppController {}
